airflow:
  connections:
    - conn_id: crypto_etl_pipe  # don't modify
      conn_type: postgres  # don't modify
      conn_host: # host.docker.internal for localhost
      conn_schema: # PostgreSQL database name
      conn_login:
      conn_password:
      conn_port:
      conn_extra:
        example_extra_field:
  pools:
    - pool_name:
      pool_slot:
      pool_description:
  variables:
    - variable_name:
      variable_value:
